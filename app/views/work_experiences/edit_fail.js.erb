$.gritter.add({
    title: '<%= t('system.system_message') %>',
    text: '<%= flash[:error]%>',
    before_open: function(){
        if($('.gritter-item-wrapper').length == 1)
        {
            return false;
        }
    }
});


